apiVersion: integreatly.org/v1alpha1
kind: Grafana
metadata:
  name: grafana-sandbox
spec:
  service:
    type: ClusterIP
  deployment:
    envFrom:
      - configMapRef:
          name: https-proxy
      - secretRef:
          name: github-auth-sandbox-20200424
    securityContext:
      runAsUser: 10000
  config:
    log:
      mode: "console"
      level: "warn"
    auth:
      disable_login_form: false
      disable_signout_menu: true
    auth.anonymous:
      enabled: false
    auth.github:
      enabled: true
      allow_sign_up: true
      scopes: user:email,read:org
      auth_url: https://github.com/login/oauth/authorize
      token_url: https://github.com/login/oauth/access_token
      api_url: https://api.github.com/user
      allowed_organizations: cybozu-private
    server:
      domain: grafana-sandbox.monitoring.stage0.cybozu-ne.co
      root_url: https://grafana-sandbox.monitoring.stage0.cybozu-ne.co
    analytics:
      check_for_updates: true
    grafana_net:
      url: https://grafana-sandbox.monitoring.svc.cluster.local
    users:
      viewers_can_edit: true
      auto_assign_org_role: Editor
  dashboardLabelSelector:
    - matchExpressions:
        - {key: app.kubernetes.io/name, operator: In, values: [grafana]}
